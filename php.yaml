title: Éxecution PHP
question: |
  <p>Qu'affiche le code suivant ?</p>
  <pre><code>function toto($obj, $fun) {
    foreach ($obj as $k => $v) {
      echo "$k is $v";
    }
    return $fun(1);
  }

  toto(array("foo" => "bar", "bar" => "foo"),
       function($val) { return $val + $val; });</code></pre>
choices:
  - answer: |
      <pre><code>2</code></pre>
  - answer: |
      <pre><code>bar is foo
      foo is bar</code></pre>
  - answer: |
      <pre><code>foo is barbar is foo</code></pre>
    correct: true
  - answer: |
      <pre><code>foo is bar
      bar is foo
      2</code></pre>
